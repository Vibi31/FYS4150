// refreshing my c++ memory 

using namespace std;

#include <iostream>

int main()
{
  // Print a message to screen
  std::cout << "Hello, World!\n";

  // Return 0 when done
  return 0;
}

// to compile its                                       g++ -c filename.cpp
//we get a filename.o object, whcih we can rename 
// to make it into an object to execute                 g++ -o filename.exe  
// we can use .exe or anything else 
//they can be both combined to run both at once         g++ test_learn.cpp -o test_learn
//to run the file we type                               ./filename.exe